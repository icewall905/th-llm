services:
  backend:
    build: ./backend
    ports:
      - "8700:8000"
    env_file: .env
    restart: unless-stopped
    volumes:
      - ./data:/app/data

  frontend:
    build: ./frontend
    ports:
      - "3700:80"
    depends_on:
      - backend
    restart: unless-stopped
